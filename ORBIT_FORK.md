# Orbit fork of just-bash

This fork is synchronized with [dbreunig/just-bash-py](https://github.com/dbreunig/just-bash-py)
through v0.2.1.

Orbit's network/curl wiring and TS-parity allow-list work landed upstream in
v0.2.0, so Orbit now pins upstream PyPI `just-bash==0.2.1` rather than this
repository. The fork remains available as a staging point for future upstream
contributions.
